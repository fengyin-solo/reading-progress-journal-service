# Bug 说明

阅读进度同步重试成功后，首轮延迟回调会覆盖新状态，同步通知也会重复发布。

## 如何触发

先启动一次阅读进度同步，接着让第二次尝试成功，最后送达首轮的处理中回调，然后同时查询详情和列表摘要。

## 错误信息

详情返回 `version=1, state=processing`，列表摘要仍为 `version=2, state=succeeded`，同一同步的通知数为 `2`。
